[package]
name = "sidecar"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1"
sidecar-proto = { path = "../sidecar-proto" }
tokio = { version = "1", features = ["full"] }
