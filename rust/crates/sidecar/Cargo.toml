[package]
name = "sidecar"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
sidecar-proto = { path = "../sidecar-proto" }
